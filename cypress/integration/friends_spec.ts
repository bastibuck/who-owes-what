describe("Friends tab", function() {
  it("should add multiple friends correctly", function() {
    cy.visit("http://localhost:3000");
    // friend tab active

    // add button disabled

    // other tabs disabled

    // enter first friend name

    // button becomes enabled

    // added friend to friend list

    // expenses tab enabled

    // add another two friends (enter and button)

    // three friends in friendlist

    expect(false).toBeTruthy;
  });

  it("should delete friends correctly", () => {
    expect(false).toBeTruthy;
  });
});
